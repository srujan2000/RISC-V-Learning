{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Riscv",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/PLIC_Interrupt/main.elf", 
            "cwd": "${workspaceFolder}",
            "miDebuggerPath": "/usr/bin/gdb-multiarch",
            "miDebuggerServerAddress": "localhost:1234",            
            "stopAtEntry": true,
            "preLaunchTask": "Run QEMU"
        }
    ]
}
